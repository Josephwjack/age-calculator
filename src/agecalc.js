export class SolarCalculator {
  constructor(age) {
    this.age = age;
  }

  mercCalc() {
    let merc = (this.age / .24);
    let mercuryAge = [];
    mercuryAge = Math.floor(merc)
      return mercuryAge;
  }
}
  